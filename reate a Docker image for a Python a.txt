reate a Docker image for a Python application repository and run the application inside a Docker container to n application stored in a local display the output. 2. Develop a Docker image for a Java application available in a local repository, compile the program inside the container, and execute it using Docker. 3. Build a Docker image for a static web application (HTML) from a local repository and deploy it using a web server container, enabling access through a browser. 4. Construct a Jenkins pipeline to package a static HTML website using Nginx, build a Docker image, and push the image to Docker Hub. Ensure the application is accessible via a web browser. 5. Construct a Maven project for a Java console application, include all required dependencies in the pom.xml, and implement a CI/CD pipeline to automate the build process. 6. Configure Jenkins to send build and deployment status notifications to Slack and enable email notifications for each stage of the CI/CD pipeline.